#pragma once
#include <glm/glm.hpp>

namespace slamd {
namespace _const {
constexpr glm::vec3 light_dir = glm::vec3(0.2f, 0.2f, 1.0f);
constexpr float default_min_brightness = 0.2f;
}  // namespace _const
}  // namespace slamd