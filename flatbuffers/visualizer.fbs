// monster.fbs
namespace slamd;

enum TreeType: byte {
    NONE = 0,
    CANVAS = 1,
    SCENE = 2
}

table Tree {
    id: int;
    tree_type: TreeType;
}

table View {
    name: string;
    tree_id: int;
}

table VizFullState {
    name: string;
    views: [View];
    trees: [Tree];
}