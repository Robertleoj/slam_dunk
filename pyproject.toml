[project]
name = "learning-opengl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fire>=0.7.0",
    "imageio>=2.37.0",
    "numpy>=2.2.4",
    "slamd @ file:///${PROJECT_ROOT}/python_bindings",
]
